<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comunicacao.bluetooth.caderneta">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Vendas"></activity>
        <activity android:name=".Caderneta"></activity>
        <activity android:name=".FluxoDeCaixa"></activity>
        <activity android:name=".Gerenciamento"></activity>
        <activity android:name=".GerenciaProdutos"></activity>
        <activity android:name=".GerenciaClientes"></activity>
        <activity android:name=".GerenciaVendas"></activity>

    </application>

</manifest>